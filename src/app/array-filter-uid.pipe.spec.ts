import { ArrayFilterUidPipe } from './array-filter-uid.pipe';

describe('ArrayFilterUidPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFilterUidPipe();
    expect(pipe).toBeTruthy();
  });
});
